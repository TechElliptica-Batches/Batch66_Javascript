import { printStar, printHash, printPercentage } from "./printLayout.js";
// function is variable in javascript

// arrows functions - shorted form

var sum = (a, b, preeti) => {
    let c = a +b;
    preeti(c);
}

let c = 20;
let d = 40;
sum(c,d, printPercentage);


// High Order function

